def middle_character(text):
    if len(text) == 0:
        return ""

    length = len(text)
    mid = length // 2

    if length % 2 == 0:
        return text[mid - 1: mid + 1]
    else:
        return text[mid]
print(middle_character("banana"))
